---
import '../styles/global.css';
import { site } from '../config/site';

const {
  title = site.name,
  description = 'Musikinstrumente unkompliziert anbieten. Fotos senden, Anfrage persönlich prüfen lassen.',
  canonical,
  robots,
  image,
  ogType = 'website',
  structuredData,
  analytics = true
} = Astro.props;
const apiBase = import.meta.env.PUBLIC_API_BASE || '';
const analyticsExcluded = ['/review/', '/admin/', '/datenschutz/', '/impressum/'];
const analyticsEnabled = analytics && !analyticsExcluded.some((path) => Astro.url.pathname.startsWith(path));
const canonicalUrl = canonical ? new URL(canonical, Astro.site) : Astro.url;
const socialImageUrl = image ? new URL(image, Astro.site) : undefined;
const organization = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: site.name,
  url: site.url,
  email: site.email,
  address: {
    '@type': 'PostalAddress',
    streetAddress: site.address.street,
    postalCode: site.address.postalCode,
    addressLocality: site.address.city,
    addressCountry: site.address.country
  },
  areaServed: {
    '@type': 'Country',
    name: 'Deutschland'
  }
};
---
<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="referrer" content="strict-origin-when-cross-origin" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    {robots ? <meta name="robots" content={robots} /> : null}
    <meta property="og:type" content={ogType} />
    <meta property="og:locale" content="de_DE" />
    <meta property="og:site_name" content={site.name} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    {socialImageUrl ? <meta property="og:image" content={socialImageUrl} /> : null}
    {socialImageUrl ? <meta property="og:image:alt" content={title} /> : null}
    <meta name="twitter:card" content={socialImageUrl ? 'summary_large_image' : 'summary'} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    {socialImageUrl ? <meta name="twitter:image" content={socialImageUrl} /> : null}
    <script type="application/ld+json" set:html={JSON.stringify(organization)}></script>
    {structuredData ? <script type="application/ld+json" set:html={JSON.stringify(structuredData)}></script> : null}
    <link rel="icon" href="/favicon.ico" />
    <title>{title}</title>
  </head>
  <body>
    {analyticsEnabled ? <script src="/js/site-analytics.js?v=20260829-1" data-api-base={apiBase} is:inline></script> : null}
    <a class="skip-link" href="#main-content">Zum Inhalt springen</a>
    <header class="site-header">
      <a class="brand" href="/">Musikinstrument <span>Ankauf</span></a>
      <nav aria-label="Hauptnavigation">
        <a href="/#so-funktionierts">So funktioniert es</a>
        <a href="/instrument-verkaufen/" class="nav-cta">Instrument anbieten</a>
      </nav>
    </header>
    <main id="main-content"><slot /></main>
    <footer class="site-footer">
      <div class="footer-brand">
        <strong>Musikinstrument Ankauf</strong>
        <span>Deutschlandweiter Ankauf und persönliche Prüfung von Instrumentenanfragen.</span>
      </div>
      <div class="footer-navs">
        <nav aria-label="Fachseiten">
          <a href="/instrumentengeschichten/">Alle Instrumentengeschichten</a>
          <a href="/geige-verkaufen/">Geige verkaufen</a>
          <a href="/cello-verkaufen/">Cello verkaufen</a>
          <a href="/bogen-verkaufen/">Bogen verkaufen</a>
          <a href="/kontrabass-verkaufen/">Kontrabass verkaufen</a>
          <a href="/kontrabassbogen-verkaufen/">Kontrabassbogen verkaufen</a>
          <a href="/instrument-geerbt/">Instrument geerbt</a>
          <a href="/instrumentengeschichten/albert-volkmann-1908/">Albert Volkmann 1908</a>
          <a href="/instrumentengeschichten/johannes-rubner-1967/">Johannes Rubner 1967</a>
          <a href="/instrumentengeschichten/f-guenter-hoyer-kontrabassbogen-um-1950/">F. Günter Hoyer um 1950</a>
          <a href="/instrumentengeschichten/august-rau-geigenbogen-um-1910/">August Rau um 1910</a>
        </nav>
        <nav aria-label="Rechtliches">
          <a href="/impressum/">Impressum</a>
          <a href="/datenschutz/">Datenschutz</a>
        </nav>
      </div>
    </footer>
  </body>
</html>
